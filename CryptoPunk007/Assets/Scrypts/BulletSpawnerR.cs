using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletSpawnerR : MonoBehaviour
{
    BulletSpawnerR bulletSrawnerR;
    // Start is called before the first frame update
    void Start()
    {
        BulletSpawnerR bulletSpawnerR = GetComponent<BulletSpawnerR>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
